{
  "name": "lingora-mock-api",
  "version": "1.0.0",
  "description": "Mock API server for Lingora frontend development",
  "main": "db.json",
  "scripts": {
    "start": "json-server --watch db.json --port 3002 --routes routes.json"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}