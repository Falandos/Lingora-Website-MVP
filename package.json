{
  "name": "lingora",
  "version": "1.0.0",
  "description": "Find professionals who speak your language",
  "scripts": {
    "mock-api": "json-server --watch mock/db.json --port 3001",
    "dev": "concurrently \"npm run mock-api\" \"cd frontend && npm run dev\""
  },
  "devDependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
